import { Suspense } from 'react'
import { JobSuccess } from '@/views/JobSuccess'

export default function JobSuccessPage() {
    return (
        <Suspense>
            <JobSuccess />
        </Suspense>
    )
}
